/**
 * item256 — No = 256 チョコレイト・ディスコの発動処理
 *
 * 各スタンドの発動能力設定。
 *
 * (org原典: newDTW_item(スタンドdisc))
 * // @org-injected
 */
import { Gvar } from '../variable'
import * as Adap from '../adapter/index'
import * as Func from '../func/index'

// No = 256 チョコレイト・ディスコの発動処理 
async function item256(this: any) {

}

export {item256}
