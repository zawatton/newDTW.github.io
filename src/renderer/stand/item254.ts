/**
 * item254 — No = 254 C・ザ・レインボーの発動処理
 *
 * 各スタンドの発動能力設定。
 *
 * (org原典: newDTW_item(スタンドdisc))
 * // @org-injected
 */
import { Gvar } from '../variable'
import * as Adap from '../adapter/index'
import * as Func from '../func/index'

// No = 254 C・ザ・レインボーの発動処理 
async function item254(this: any) {

}

export {item254}
