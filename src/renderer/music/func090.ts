import { Gvar } from '../variable'
import * as Adap from '../adapter/index'

// 旧 func090
async function func090(this: any) {
        Adap.dbgprt(90);
        Adap.DSPLAY(141);
        return;
}

export {func090}
