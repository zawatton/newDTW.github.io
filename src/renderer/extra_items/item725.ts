/**
 * item725 — No = 725 オレっちのコートを使用した時の効果 (なし)
 *
 * (org原典: newDTW_item(その他))
 * // @org-injected
 */
import { Gvar } from '../variable'
import * as Adap from '../adapter/index'
// No = 725 オレっちのコートを使用した時の効果 (なし)
async function item725(this: any) {

}

export {item725}
