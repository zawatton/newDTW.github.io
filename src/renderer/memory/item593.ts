/**
 * item593 — No = 593 リンゴォのdiscの効果
 *
 * (org原典: newDTW_item(記憶disc))
 * // @org-injected
 */
import { Gvar } from '../variable'
import * as Adap from '../adapter/index'
// No = 593 リンゴォのdiscの効果
async function item593(this: any) {

}

export {item593}
